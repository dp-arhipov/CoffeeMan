import React from 'react';
import './styles.scss'
const Loader = () => {
    return (
        <div className='loader'>
            <div className="loader-body"></div>
        </div>
    );
};

export default Loader;